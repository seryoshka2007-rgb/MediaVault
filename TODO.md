# TODO (near-term)

- [ ] Generate Alembic baseline: `alembic revision --autogenerate -m "baseline"`.
- [ ] CRUD dialogs (add/edit) wired to EntryService.
- [ ] Status + favourites filter in the main window.
- [ ] "Open link" action with URL validation.
- [ ] First real provider + rate limiting/timeout/caching.
- [ ] JSON export/import for safe sync (see docs/SYNC_WARNING.md).
- [ ] CI: ruff + mypy + pytest on push.
